targets:
  $default:
    builders:
      slang_build_runner:
        options:
          namespaces: true
          output_directory: lib/src/i18n
          base_locale: en
          fallback_strategy: base_locale
          translation_class_visibility: public